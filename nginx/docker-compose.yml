# docker-compose.yml
version: '1'
networks:
  app-network:
    name: common_proxy
    driver: bridge

services:
  nginx:
    container_name: nginx
    image: "nginx:1.20.2-alpine"
    restart: unless-stopped
    tty: true
    volumes:
      - ./:/var/www
      - ./conf.d/:/etc/nginx/conf.d/
    ports:
      - "80:80"
      - "443:443"
    networks:
      - app-network